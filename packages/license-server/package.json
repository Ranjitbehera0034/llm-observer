{
    "name": "@llm-observer/license-server",
    "version": "1.9.0",
    "description": "Cloud relay for LLM Observer payment webhooks and license key delivery",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vercel dev",
        "build": "tsc",
        "deploy": "vercel --prod",
        "test": "vitest run --passWithNoTests"
    },
    "dependencies": {
        "resend": "^4.0.0"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.0.0",
        "vitest": "^3.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
