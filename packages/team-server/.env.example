# LLM Observer Team Server — Environment Variables

## REQUIRED
# MongoDB Atlas connection string
# Get from: https://cloud.mongodb.com → Connect → Drivers
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/llm-observer-team

## Optional
PORT=4002
NODE_ENV=production
