# @llm-observer/proxy

Core proxy engine that intercepts LLM API calls, counts tokens, calculates costs, and logs everything locally.

This is an internal package — not published to npm directly. It's consumed by the `llm-observer` CLI package.
