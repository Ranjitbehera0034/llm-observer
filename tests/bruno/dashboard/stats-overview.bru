meta {
  name: Get Stats Overview
  type: http
  seq: 3
}

get {
  url: {{dashboard_url}}/api/stats/overview
  body: none
  auth: none
}

params:query {
  projectId: default
}

assert {
  res.status: eq 200
  res.body.todaySpendUsd: isDefined
  res.body.totalRequestsToday: isDefined
}
